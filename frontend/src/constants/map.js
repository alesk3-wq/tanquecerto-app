// Brasília — centro padrão quando não há posição do usuário
export const DEFAULT_CENTER = [-15.78, -47.93];

// Raio (km) para considerar que o usuário está no pátio de um posto já cadastrado
export const REFUEL_CHECK_RADIUS_KM = 0.2;
